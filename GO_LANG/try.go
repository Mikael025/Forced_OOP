package main

import (
	"fmt"
	// "time"
	// "math/rand"
)

func main () {
	// rand.Seed(time.Now().UnixNano())

	// random := rand.Intn(100)
	// fmt.Println(random)

	luas := luaspersegi(5)
	fmt.Println(luas)
}